← [Design Skill 主索引](../SKILL.md)

# 变量映射规则 · 硬约束

> ⚠️ 本文档定义 OpenEuler 设计稿中**所有**字号、行高、间距、栅格、图标尺寸的合法取值。AI 在生成或修改 Pixso 设计稿时**必须严格选用本表中的值**，禁止四舍五入、推断、造数，或使用"看起来合理"的非清单数值。任何不在本表内的几何数值都视为非法。

---

## 0. 颜色 Token 常见错误（生成HTML前必须拉取上游Token）

> 🔴 **AI生成HTML页面时，必须先通过 WebFetch 拉取 `openeuler-token.json`（见 SKILL.md 数据资源），将 JSON 中的颜色变量翻译为 `:root` CSS 变量。禁止凭训练数据"猜测"颜色值。**

> ⚠️ 以下仅列出**最常出错**的Token对照，完整值以拉取的上游JSON为准。

| Token | 语义 | 常见错误 | 正确理解 |
|-------|------|---------|---------|
| `--o-color-fill1` | **页面背景色** | ❌ `rgb(255,255,255)` / `#FFFFFF`（纯白） | fill1 是**浅灰**页面背景，不是纯白！纯白是 fill2 |
| `--o-color-fill2` | **卡片/容器背景色** | ❌ 与 fill1 混淆 | fill2 才是**纯白**卡片背景 |

**⚠️ 核心规则**：
- `body { background-color: var(--o-color-fill1); }` → 页面整体浅灰背景
- 卡片/表格/输入框 `{ background-color: var(--o-color-fill2); }` → 纯白容器背景
- 如果页面看起来"全白没有层次"，说明 fill1 写错了

---

## 1. 断点定义

| 设备 | 屏宽范围 | 使用列 |
|---|---|---|
| **MB（移动端）** | 360 ~ 600px | 下方所有表格的 **MB** 列 |
| **PC（桌面端）** | > 1680px | 下方所有表格的 **PC** 列 |

**核心原则**：

- 单个设备类别内（如 MB 360→600px），**字号、间距均不做变化**，只做横向布局拉伸（以 MB 端为主进行设计）
- 跨设备类别（PC ↔ MB）切换时，按下方各表执行 1:1 映射

---

## 2. 文字（字号 / 行高）

> 🔴 **本节最高优先级铁律**：生成**任何**可编辑的 Pixso 设计稿时，**全局字体一律使用「鸿蒙黑体（HarmonyOS Sans / HarmonyHeiTi）」**，无任何例外。禁止使用 PingFang、苹方、思源、微软雅黑、Inter、Roboto、系统默认字体或任何其他字族。设计稿中每一个文字图层的 `font_family` 必须显式设为鸿蒙黑体。

下表中"角色"指文字在页面信息层级中的语义身份。**字号与行高在同一行内成对锁定，禁止拆分搭配**。

| 角色 | PC（字号 / 行高） | MB（字号 / 行高） | MB 上的角色重命名 |
|---|---|---|---|
| 一级数据展示 | 56px / 80 | 22px / 30 | — |
| 二级数据展示 | 48px / 64 | 20px / 28 | — |
| 三级数据展示（楼层标题） | 40px / 56 | 18px / 26 | — |
| 一级标题 | 32px / 44 | 18px / 26 | — |
| 二级标题 | 24px / 32 | 16px / 24 | — |
| 三级标题（列表卡片 < 3 列） | 22px / 30 | 16px / 24 | — |
| 四级标题（列表卡片 = 4 列） | 20px / 28 | 16px / 24 | — |
| 大号正文 | 18px / 26 | 14px / 22 | — |
| 常规正文 / 标题辅助信息 | 16px / 24（段高 12px） | 14px / 22 | 常规正文 |
| 提示文本 1 | 14px / 22 | 12px / 18 | 标题辅助信息 |
| 提示文本 2 | 12px / 18 | 10px / 16 | — |

**约束**：

- ✅ PC 合法字号集合：`{56, 48, 40, 32, 24, 22, 20, 18, 16, 14, 12}`
- ✅ MB 合法字号集合：`{22, 20, 18, 16, 14, 12, 10}`
- ✅ 字号与行高**必须同行配对**：PC 32px 行高只能是 44；MB 16px 行高只能是 24
- ❌ 禁止使用 13 / 15 / 17 / 19 / 21 / 23 / 26 / 28 / 30 / 36 等中间字号
- ❌ 禁止跨设备混用：MB 设计稿出现 32px/44 行高 = 错

---

## 3. 间距

| Token | PC | MB |
|---|---|---|
| 间距 1 | 4px | 4px |
| 间距 2 | 8px | 8px |
| 间距 3 | 12px | 8px |
| 间距 4 | 16px | 8px |
| 间距 5（一般用于组件与组件之间的**左右**间距） | 24px | 12px |
| 间距 6（一般用于组件与组件之间的**上下**间距） | 32px | 12px |
| 间距 7（楼层标题到楼层内容间距） | 40px | 12px |
| 间距 8 | 48px | 16px |
| 间距 9 | 64px | 24px |
| 间距 10（楼层之间的距离） | 72px | 32px |

**约束**：

- ✅ 所有 `padding` / `margin` / `gap` 必须从对应设备列中选择
  - PC 合法集合：`{4, 8, 12, 16, 24, 32, 40, 48, 64, 72}`
  - MB 合法集合：`{4, 8, 12, 16, 24, 32}`
- ✅ 楼层标题到内容的间距**固定**使用**间距 7**（PC 40px / MB 12px）
- ✅ 楼层之间的距离统一使用**间距 10**（PC 72px / MB 32px）
- ❌ 禁止 6 / 10 / 14 / 18 / 20 / 28 / 36 / 56 等未列出数值
- ❌ MB 端禁止出现 40 / 48 / 64 / 72px 等 PC 专属数值

---

## 4. 栅格

### 4.1 实际布局速查（生成页面时使用）

> 🔴 **生成 Pixso 设计稿时，画布尺寸与内容区位置必须严格按下表落实**。这是 AI 真正动手时的取值表。底层栅格结构定义见 §4.2（仅参考，不直接用于布局）。

| 项 | PC | MB |
|---|---|---|
| 画布总宽度 | 1920px | 360px |
| 内容区宽度 | 1488px | 312px |
| 内容区距画布两边边距 | 216px | 24px |
| 栅格列数 | 24 列 | 4 列 |
| 水槽宽度（card 与 card 的上下左右间距） | 32px | 12px |

**校验**：内容区宽度 + 两边边距 × 2 = 画布总宽度
- PC：1488 + 216 × 2 = 1920 ✓
- MB：312 + 24 × 2 = 360 ✓

### 4.1.1 ⚠️ HTML/CSS 实现翻译（极易出错，必须严格遵守）

> 🔴 **AI生成HTML页面时，每个楼层的内容容器必须严格使用以下CSS值**。禁止使用任何"常见默认值"（如1200px、1140px等），禁止用小间距padding（如24px）代替PC端边距。AI训练数据中大量存在Bootstrap等框架的默认容器宽度，这些值**不属于**openEuler设计规范，必须主动排斥。

**PC 端楼层内容容器（必须使用）**：

> ⚠️ **禁止使用 `margin: 0 auto` + `padding: 0 216px` 组合！** 在 `box-sizing: border-box` 下，padding 会被吃进 max-width 内，导致内容区实际只有 1488-432=1056px，且 `margin: 0 auto` 会产生额外不可控的 margin。**必须使用固定 margin-left/margin-right 方案。**

```css
.floor-container {
    width: 1488px;                    /* 内容区固定宽度，撑满 */
    max-width: 1488px;                /* ⚠️ 禁止使用 1200px / 1140px / 960px 等常见框架默认值 */
    margin-left: 216px !important;    /* 固定左边距 */
    margin-right: 216px !important;   /* 固定右边距 */
    padding: 0 !important;            /* ⚠️ 禁止使用 padding 控制楼层边距！padding 仅用于组件内部间距 */
}
```

**MB 端楼层内容容器（必须使用）**：

```css
.floor-container-mb {
    width: 312px;                     /* 内容区固定宽度 */
    max-width: 312px;                 /* ⚠️ 禁止使用 360px（那是画布宽度，不是内容区宽度） */
    margin-left: 24px !important;     /* 固定左边距 */
    margin-right: 24px !important;    /* 固定右边距 */
    padding: 0 !important;            /* ⚠️ 禁止使用 padding 控制楼层边距 */
}
```

**常见错误对照表**：

| # | 错误写法 | 正确写法 | 错误原因 |
|---|---------|---------|---------|
| 1 | `max-width: 1200px` | `max-width: 1488px` | 1200px是Bootstrap默认值，不是openEuler规范 |
| 2 | `max-width: 1140px` | `max-width: 1488px` | 1140px是另一常见框架值，不合规 |
| 3 | `max-width: 960px` | `max-width: 1488px` | 960px是老版Bootstrap值，不合规 |
| 4 | `margin: 0 auto` + `padding: 0 216px` | `margin-left/right: 216px` + `padding: 0` | border-box下padding吃进max-width，内容区变窄+auto产生额外margin |
| 5 | `padding: 0 24px`（PC端） | `margin-left/right: 216px` + `padding: 0` | 24px是MB端边距，PC端必须用margin 216px |
| 6 | `padding: 0 32px`（PC端） | `margin-left/right: 216px` + `padding: 0` | 32px是组件间距，不是楼层边距 |
| 7 | `max-width: 360px`（MB端） | `max-width: 312px` | 360px是MB画布宽度，内容区宽度是312px |
| 8 | 不设max-width，只用padding | width + max-width + margin-left/right + padding:0 | 必须四件套齐全才能正确布局 |

**验证公式**：`margin-left + width + margin-right = 画布宽度`
- PC：216 + 1488 + 216 = **1920** ✓
- MB：24 + 312 + 24 = **360** ✓

### 4.2 设计系统栅格 spec（底层定义，参考用）

> ℹ️ 下方 JSON 为设计系统底层栅格结构定义，描述**画布栅格基础参数**。**`margin` 字段 ≠ §4.1 中"内容区距画布两边边距"**（前者是栅格安全间距 PC 64 / MB 24，后者是实际内容布局边距 PC 216 / MB 24）。
>
> 落地页面时**以 §4.1 为准**，本节仅作为底层 spec 参考。`vp` 为 HarmonyOS 密度无关单位，与 `px` 1:1 对待。

```json
"grid": {
  "pc": {
    "breakpoint": "1920px",
    "columns": 24,
    "gutter": "32vp",
    "margin": "64px",
    "width": "1920px"
  },
  "mb": {
    "breakpoint": "360px",
    "columns": 4,
    "gutter": "12vp",
    "margin": "24px",
    "width": "360px"
  }
}
```

字段含义：

| 字段 | 含义 |
|---|---|
| `breakpoint` | 设计稿对齐的基准断点（PC 1920 / MB 360），即设计稿应以此宽度为基础进行布局 |
| `width` | 画布固定宽度（生成 Pixso 设计稿时的 frame 宽度） |
| `columns` | 栅格列数（PC 24 / MB 4） |
| `gutter` | 列间距 / 水槽宽度（PC 32 / MB 12） |
| `margin` | 栅格安全间距（PC 64 / MB 24）。**注：此值 ≠ §4.1 实际内容布局边距（PC 216 / MB 24），生成页面时以 §4.1 为准** |

**约束**：

- ✅ PC **必须**使用 24 栅格，MB **必须**使用 4 栅格
- ✅ PC 整页画布宽度**固定 1920px**，MB 整页画布宽度**固定 360px**
- ✅ PC 实际内容区**宽度固定 1488px、距画布两边各 216px**；MB 实际内容区**宽度固定 312px、距画布两边各 24px**（见 §4.1）
- ✅ 卡片之间间距严格使用 PC 32px / MB 12px
- ❌ 禁止使用 6 / 8 / 12 / 16 / 20 等其他栅格数
- ❌ 禁止使用 1440 / 1680 / 375 / 414 等非基准画布宽度
- ❌ MB 端不要把卡片间距设成 16 / 24 / 32px

---

## 5. 图标尺寸

| Token | PC | MB |
|---|---|---|
| 尺寸 1 | 16px | 16px |
| 尺寸 2 | 20px | 24px |
| 尺寸 3 | 24px | 24px |
| 尺寸 4 | 32px | 24px |
| 尺寸 5 | 40px | 32px |
| 尺寸 6 | 48px | 32px |
| 尺寸 7 | 56px | 40px |
| 尺寸 8 | 64px | 40px |

**约束**：

- ✅ 图标必须为正方形（`width === height`）
- ✅ PC 合法集合：`{16, 20, 24, 32, 40, 48, 56, 64}`
- ✅ MB 合法集合：`{16, 24, 32, 40}`
- ⚠️ **注意**：尺寸 2 在 MB 上（24px）**大于** PC（20px），这是有意设计，不要"修正"为 20px
- ❌ 禁止 18 / 22 / 26 / 28 / 36 / 44 / 52 等未列出尺寸
- ❌ MB 端禁止出现 20 / 48 / 56 / 64px 等 PC 专属图标尺寸

---

## 6. AI 自检清单（每次生成 / 修改前必走）

```
□ 1. 当前目标设备：[ ] MB   [ ] PC   → 已锁定对应列
□ 2. 所有字号是否来自该设备列中的字号集合？
□ 3. 字号与行高是否成对（未拆分搭配）？
□ 4. 所有 padding / margin / gap 是否来自该设备列中的间距集合？
□ 5. 栅格列数 / 水槽 / 边距是否符合该设备列？
□ 6. 图标是否正方形，且尺寸来自该设备列的图标集合？
□ 7. 是否存在"看起来合理但未列出"的数值？
□ 8. 【落地页】页面结构是否符合：导航 → Banner → 楼层(s) → 页脚？
□ 9. 【落地页】楼层间距是否统一使用间距 10（PC 72px / MB 32px）？
□ 10. 【HTML栅格】每个楼层内容容器 width + max-width 是否为 1488px（PC）/ 312px（MB）？
□ 11. 【HTML栅格】每个楼层内容容器是否使用 margin-left: 216px + margin-right: 216px（PC）/ margin-left: 24px + margin-right: 24px（MB）？（禁止 `margin: 0 auto` + `padding: 0 216px` 组合）
□ 12. 【HTML栅格】每个楼层内容容器 padding 是否为 0？（禁止用 padding 控制楼层边距）
□ 13. 【HTML栅格】是否误用 1200px / 1140px / 960px 等常见框架默认值？
□ 14. 【HTML栅格】验证公式：margin-left + width + margin-right = 画布宽度（PC: 216+1488+216=1920 / MB: 24+312+24=360）？
□ 15. 【HTML颜色】`:root` 中的颜色值是否来自上游Token（openeuler-token.json），而非凭训练数据猜测？（尤其检查 `--o-color-fill1` 是否误写为纯白 `#FFFFFF`）
□ 16. 【HTML颜色】`body` 的 `background-color` 是否使用 `var(--o-color-fill1)`？（禁止 `#FFFFFF` / `white`）
```

任一项不通过 → **立即回滚到清单值**，禁止"差不多"。无法对应到清单时，停下询问用户，**不要自行造数**。

---

## 7. 不在本表内的属性

下列属性**不受**本变量映射规则约束，按各自来源处理：

| 属性 | 来源 |
|---|---|
| 色值（`color-*`） | 远端 Token JSON，见 [SKILL.md 数据资源](../SKILL.md#数据资源) |
| 字重（`font_weight-*`） | 远端 Token JSON |
| 阴影（`shadow-*`） | 远端 Token JSON |
| 圆角 | 见 [atomic-components/](../atomic-components/) 各组件文档（按钮 4 / 16 / 100px、输入框/卡片/Tag 4px 等） |
| 按钮 / 输入框 / Tag 等**组件级**几何参数 | 见 [atomic-components/](../atomic-components/) 各组件文档 |
| 描边宽度 | 全局 1px INSIDE，不随设备变化 |

---

## 8. 落地页模板规则（page-building）

> ⚠️ 本节定义**标准落地页**的页面结构与区块顺序约束。AI 在生成或补全落地页设计稿时，**必须**严格遵循下方结构，不得随意增删必选区块或调整顺序。

### 8.1 标准结构（从上到下）

| 顺序 | 区块 | 必选 / 可选 | 说明 |
|---|---|---|---|
| 1 | **顶部导航** | 必选 | 页面第一个内容区块，常驻吸顶 |
| 2 | **Banner** | 可选 | 若存在，必须紧贴导航，位于页面第二个区块 |
| 3 | **楼层（可多个）** | 必选 | 详见 §8.2；楼层间距遵循间距 10 |
| 4 | **页脚** | 必选 | 常驻页面最底部 |

### 8.2 楼层内部结构

每个完整楼层由以下子结构组成：

```
楼层
├── 楼层标题              （必选）
├── 楼层标题描述性文本    （可选）
└── 楼层内容
    ├── 操作 Tab          （可选）
    ├── 卡片布局 / 表格等容器  （内容主体）
    └── 操作按钮          （可选）
```

### 8.3 约束

- ✅ **顶部导航**：必须是页面第一个内容区块，且**常驻吸顶**（`position: sticky / fixed`）
- ✅ **Banner**：若存在，必须是页面第二个内容区块，**紧贴导航**，不得有其他区块插入其间
- ✅ **楼层数量**：一个页面可包含**多个**楼层，数量不限
- ✅ **楼层间距**：相邻楼层之间统一使用**间距 10**（PC **72px** / MB **32px**），与 §3 保持一致
- ✅ **页脚**：每个页面必须有页脚，**常驻页面最底部**，不得被其他内容覆盖
- ❌ 禁止在导航之前放置任何内容区块
- ❌ 禁止在导航与 Banner 之间插入任何其他区块（若 Banner 存在）
- ❌ 禁止省略顶部导航或页脚
- ❌ 禁止楼层之间使用非间距 10 的数值（如 PC 64px / MB 24px 等）

---

## 9. 突破规则的唯一通道

如确需使用非清单值（如品牌特殊页面、营销 banner 等），**必须**在回复中显式声明：

> ⚠️ 此处使用了非标准值 `XXpx`，不在变量映射白名单内。原因：[说明]。是否确认覆盖？

由用户拍板后才能落到设计稿中。**默认行为永远是回退到最近的合法值**，而不是静默放行。

---

---

# 组件组合规则 · 硬约束

> ⚠️ 本文档定义 OpenEuler 设计稿中**组件排布、嵌套与组合**的合法规则。AI 在生成或修改 Pixso 设计稿时**必须严格遵守**，违反任何一条均视为非法布局，必须自动修正或抛出规范提醒。

---

## 1. 通用规则

### 1.1 Button（按钮）

| 规则 | 说明 |
|---|---|
| **对齐** | 按钮无论置于容器内还是页面背景中，**一律左对齐** |
| **排列优先级** | 多个按钮共存时，从左到右顺序为：`solid`（强调）→ `outline`（普通）→ `text`（文本） |
| **Solid 数量** | 同一组按钮中，`solid` 变体**有且只有一个** |
| **按钮间距** | 同组按钮之间的水平间距**固定 24px** |
| **与其他内容的上下间距** | 按钮（组）与周围其他内容或组件的上下间距**固定 24px** |

### 1.2 组件与栅格

- ✅ **所有组件必须挂靠栅格容器**，严格对齐内容区的栅格列
- ❌ **禁止组件游离在页面空白区域独立摆放**（即不得出现脱离栅格的浮动元素）

### 1.3 Card（卡片）

- ✅ 卡片与卡片之间的间距**固定使用 32px**（与硬约束 §4.1 水槽宽度一致）
- ❌ 禁止使用 16 / 20 / 24 / 40px 等非标准卡片间距

---

## 2. 禁止组合黑名单（Skill 自动校验拦截）

> 下列组合一经发现，Skill 自动拦截并提示修正，**不得静默放行**。

| # | 禁止行为 |
|---|---|
| 1 | ❌ **L-Banner 塞进卡片、弹窗、子内容区块**（L-Banner 仅允许出现在页面顶级区块中） |
| 2 | ❌ **同一区块内 L + M + S 不同尺寸 Banner 并排** |
| 3 | ❌ **按钮放在标题 / 富文本上方**（按钮必须位于对应内容的下方） |
| 4 | ❌ **表单按钮分散在各表单项中间**（表单按钮统一置于表单末尾） |
| 5 | ❌ **卡片内部横向混放多种不同规格组件** |

---

## 3. 页面生成时 AI Skill 自动校验项

> 生成或修改设计稿时，Skill 必须在产出前完成以下校验，不通过则自动修正或抛出规范提醒：

```
□ 1. Banner 尺寸与所在页面 / 容器是否匹配（不允许 L-Banner 嵌入子区块）？
□ 2. 同区块内 Banner 尺寸是否统一（不允许多尺寸并排）？
□ 3. 按钮排列顺序：solid → outline → text，且 solid 有且只有一个？
□ 4. 按钮对齐方式是否为左对齐？
□ 5. 按钮间距及按钮与周围内容上下间距是否均为 24px？
□ 6. 所有组件是否挂靠栅格容器（无游离元素）？
□ 7. 卡片间距是否统一为 32px？
□ 8. 组件排布顺序、嵌套层级是否合法（无黑名单组合）？
□ 9. 【HTML栅格】楼层内容容器是否使用 margin-left/right: 216px（PC）/ 24px（MB）+ padding: 0？（禁止 `margin: 0 auto` + `padding: 0 216px` 组合）
□ 10. 【HTML栅格】是否误用 1200px / 1140px / 960px 等常见框架默认值？
```

任一项不通过 → **自动修正到合规状态**，无法自动修正时停下询问用户，**禁止静默放行**。